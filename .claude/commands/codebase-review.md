---
description: コード全体をレビューして指摘一覧を docs/review-findings.md に保存する(一覧が既にあれば未対応の指摘を1件解消する)
---

`skills/codebase-review/SKILL.md` の手順に従ってください。

指定(あれば): $ARGUMENTS

- `docs/review-findings.md` が**無い**場合(または全面再レビューの指定がある場合): コードベース全体をレビューし、指摘一覧を重大度順で保存して報告してください。このモードでは修正しません。
- **有る**場合: `ステータス: 未対応` の指摘を1件だけ、種別に応じた規律(バグ → 再現ファースト / リファクタ → 挙動保存 / 設計 → 選択肢の提示のみ)で解消し、ステータスを更新して停止してください。
